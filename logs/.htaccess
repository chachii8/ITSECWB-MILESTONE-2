# Block direct HTTP access to log files.
# PHP (file_put_contents, etc.) can still write — this only applies to browser/URL requests.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
